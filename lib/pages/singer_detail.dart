import 'package:flutter/material.dart';
class SingerDetail extends StatelessWidget {
  const SingerDetail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('singer_detail'),
    );
  }
}