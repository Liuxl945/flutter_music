import 'package:flutter/material.dart';
class RecommendDetail extends StatelessWidget {
  const RecommendDetail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('recommend_detail'),
    );
  }
}