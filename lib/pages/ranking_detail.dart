import 'package:flutter/material.dart';
class RankingDetail extends StatelessWidget {
  const RankingDetail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ranking_detail'),
    );
  }
}